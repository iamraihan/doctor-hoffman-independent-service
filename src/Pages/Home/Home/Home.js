import React from 'react';

const Home = () => {
    return (
        <div className=" bg-gradient-to-r from-cyan-100 to-blue-100" style={{ height: 'calc(100vh - 80px)' }}>
            <button>Book Appointment</button>
            <button>Get Special Offer</button>

        </div>
    );
};

export default Home;